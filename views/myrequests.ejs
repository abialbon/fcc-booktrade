<!doctype html>
<html lang="en">
<%- include('partials/head.ejs') %>
<body>
<%- include('partials/navbar.ejs') %>

<%- include('partials/section-head.ejs', { svgurl: './svg/butterfly.svg', titletext: 'My Requests' })%>

<div class="container">
    <div class="tabs">
        <ul>
            <li><a href="/mybooks">My books</a></li>
            <li class="is-active"><a>My Requests</a></li>
            <li><a href="/requests">Requests to me</a></li>
        </ul>
    </div>
</div>

<section>
    <div class="container">
        <div class="columns is-centered">
            <div class="column is-three-quarters-tablet">
                <!-- ➿ looping the returned results here... -->
                <% if (books) { %>
                <% for(var i = 0; i < books.length; i++) { %>
                <div class="box">
                    <article class="media">
                        <div class="media-left">
                            <figure class="image is-64x64">
                                <img class="book-thumbnail" src="<%= books[i].book.imgurl %>" alt="">
                            </figure>
                        </div>
                        <div class="media-content">
                            <div class="content">
                                <p>
                                    <strong class="book-title"><%= books[i].book.title %></strong>
                                    <small class="book-authors">
                                        <% if (books[i].book.authors) { %>
                                        <%= books[i].book.authors %>
                                        <% } %>
                                    </small>
                                    <br/>
                                    <span class="book-description">
                                        <% if (books[i].book.description) { %>
                                        <%= books[i].book.description.slice(0, 140) + '...' %>
                                        <% } %>
                                    </span>
                                </p>
                            </div>
                            <span class="tag">Requested on <%= books[i].date.toDateString() %></span>
                            <span class="tag">Status <%= books[i].status %></span>
                        </div>
                    </article>
                </div>
                <% }} %>
                <!-- The loop -->
            </div>
        </div>
    </div>
</section>
<script>

</script>
</body>
</html>